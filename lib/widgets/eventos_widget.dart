import 'package:flutter/material.dart';

class eventos_widget extends StatelessWidget {
  const eventos_widget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}