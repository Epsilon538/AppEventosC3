import 'package:flutter/material.dart';

class detallesEvento extends StatelessWidget {
  const detallesEvento({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('holi'),
        ),
      ),
    );
  }
}