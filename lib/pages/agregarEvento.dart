import 'package:flutter/material.dart';

class AgegarEventos extends StatelessWidget {
  const AgegarEventos({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}